/* eslint-disable @next/next/no-img-element */
import style from '../../styles/PedidoStyle/Pedido.module.sass'
type listType = {
    list: string[]
    imgname?: string
    menu?: string
}

export default function Card({ list, imgname, menu }: listType) {
    return (
        <>

        </> 

    )
}